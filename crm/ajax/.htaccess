# CRM AJAX endpoints are same-origin only; auth + CSRF are enforced in PHP
# (init.php + auth_gate.php). No CORS headers, no debug flags here.
<Files "*.php">
    Order Allow,Deny
    Allow from all
</Files>
